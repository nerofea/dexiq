// Mock data oracle agent

export function getDataOracleInput(): string {
    // Example: Simulated crypto market event
    return `Bitcoin surged 5% in the last hour after a major ETF approval. Trading volume spiked across major exchanges.`;
}
